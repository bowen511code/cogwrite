services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: cogwrite
      POSTGRES_PASSWORD: cogwrite
      POSTGRES_DB: cogwrite
    ports:
      - "5432:5432"
    volumes:
      - cogwrite_db:/var/lib/postgresql/data
volumes:
  cogwrite_db:
